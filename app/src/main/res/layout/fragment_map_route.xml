<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MapRouteFragment">

    <LinearLayout
        android:id="@+id/inputLayout"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:padding="12dp"
        android:background="@drawable/bg_input_card"
        android:elevation="8dp"
        android:layout_marginTop="40dp">

        <EditText
            android:id="@+id/etStart"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="📍 Nhập điểm đi (lat,lng)"
            android:padding="12dp"
            android:background="@drawable/bg_edittext"
            android:textSize="14sp" />

        <EditText
            android:id="@+id/etEnd"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="🎯 Nhập điểm đến (lat,lng)"
            android:padding="12dp"
            android:background="@drawable/bg_edittext"
            android:layout_marginTop="8dp"
            android:textSize="14sp" />

        <Button
            android:id="@+id/btnFindRoute"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:background="@drawable/bg_button"
            android:padding="12dp"
            android:backgroundTint="@null"
            app:backgroundTint="@null"
            android:text="🚍 TÌM TUYẾN"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- 🗺️ Thêm Google Map Fragment -->
    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_above="@+id/busListContainer"
        android:layout_below="@id/inputLayout"
        android:layout_marginBottom="0dp" />

    <!-- 🚍 Danh sách tuyến hoặc EmptyView -->
    <FrameLayout
        android:id="@+id/busListContainer"
        android:layout_width="match_parent"
        android:layout_height="196dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="88dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/white"
            android:elevation="8dp" />

        <include
            android:id="@+id/emptyView"
            layout="@layout/empty_view"
            android:visibility="gone" />
    </FrameLayout>
</RelativeLayout>
